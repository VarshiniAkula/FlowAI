# OS / editor junk
.DS_Store
*.log

# Env files (never commit secrets)
.env
.env*.local

# Dependencies & build output (also covered per-app)
node_modules/
.next/
dist/
.turbo/
.vercel/

# Supabase local state
flowmind/supabase/.temp/
flowmind/supabase/.branches/
flowmind/supabase/.env.local

# Claude Code local state
.claude/worktrees/
.claude/settings.local.json
