{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd flowmind && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd flowmind && pnpm --filter @flowmind/web run build",
  "outputDirectory": "flowmind/apps/web/.next"
}
